streamlit
numpy
mediapipe
streamlit-webrtc
av

# 학습 기반 위험도 모델(risk_model.app_py_adapter)에 필요
scikit-learn
pandas
joblib

# 참고: opencv-python-headless는 의도적으로 뺐습니다.
# mediapipe가 의존성으로 opencv-contrib-python을 자동 설치하는데,
# 여기에 opencv-python-headless까지 같이 넣으면 두 패키지가 서로 다른
# 'cv2' 모듈을 같은 경로에 설치하려 하면서 충돌해 data/ 폴더(haarcascade
# xml 등)가 깨지는 문제가 있었습니다. packages.txt에 libgl1,
# libglib2.0-0t64를 추가해뒀기 때문에, opencv-contrib-python 하나만으로
# GUI 관련 시스템 의존성도 문제없이 동작합니다.
